{
  "name": "shopify-ucp-demo-mcp",
  "version": "1.0.0",
  "description": "Remote MCP server for Shopify UCP - product search and checkout via AI agents",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:self-test": "tsc --noEmit -p tsconfig.harness.json",
    "typecheck:harness": "tsc --noEmit -p tsconfig.harness.json",
    "test": "tsx --test tests/catalog-extension.test.ts",
    "self-test": "tsx harness/run-cases.ts",
    "harness": "tsx harness/run-cases.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.28.0"
}
